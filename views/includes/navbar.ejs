<nav class="site-nav">
  <div class="nav-inner">
    <a class="nav-brand" href="/">
      <span class="brand-mark"><i class="fa-regular fa-compass"></i></span>
      <span class="brand-text">BNB Couples</span>
    </a>
    <div class="nav-links">
      <a class="nav-link" href="/">Home</a>
      <a class="nav-link" href="/listings">All Listings</a>
      <% if (currentUser) { %>
        <a class="nav-link" href="/listings/new">Add Listing</a>
      <% } %>
    </div>
    <div class="nav-actions">
      <% if (currentUser) { %>
        <form action="/logout" method="post">
          <button class="btn-outline" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="btn-outline" href="/login">Login</a>
      <% } %>
    </div>
  </div>
</nav>